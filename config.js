// Configuração da versão online (Supabase). Com os dois campos vazios, o sistema roda no modo local (dados só neste navegador).
// Preencha com a URL do projeto (https://xxxxxxxx.supabase.co) e a chave "publishable" ou "anon public"
// (Supabase → Project Settings → API Keys). Essa chave pode ficar no site: o acesso aos dados é controlado pelo login e pelas políticas do banco.
window.MR_CONFIG = {
  url: 'COLE_AQUI_A_URL_DO_PROJETO',
  anonKey: 'COLE_AQUI_A_CHAVE_PUBLISHABLE'
};
